<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leidong.openldplayer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".PlayerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--注册界面-->
        <activity
            android:name=".activities.register.RegisterActivity"
            android:screenOrientation="portrait"></activity>
        <!--登录界面-->
        <activity
            android:name=".activities.login.LoginActivity"
            android:screenOrientation="portrait" />
        <!--欢迎页-->
        <activity
            android:name=".activities.splash.SplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--主界面-->
        <activity
            android:name=".activities.main.MainActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden|adjustUnspecified" />
        <!--音乐播放歌词展示界面-->
        <activity android:name=".activities.playsong.lyrics.PlaySongLyricsActivity"
            android:screenOrientation="portrait" />
        <!--音乐播放无歌词界面-->
        <activity android:name=".activities.playsong.nolyrics.PlaySongNoLyricsActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>